# Normalize line endings to LF in the repository, regardless of any
# individual contributor's local core.autocrlf setting.
* text=auto eol=lf

# Known binary types -- never touch these.
*.png binary
*.gif binary
*.pyc binary
